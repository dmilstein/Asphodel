-- An instance of a game
CREATE TABLE games (
  game_id INTEGER AUTO_INCREMENT PRIMARY KEY
  -- Add turn frequency or some such
  -- Add whose turn it is, if there are turns
  -- Add timestamps for created, last move made, etc
) ENGINE=InnoDB;